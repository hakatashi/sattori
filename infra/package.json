{
  "name": "@sattori/infra",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "sattori": "bin/sattori.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "synth": "cdk synth",
    "deploy": "cdk deploy",
    "test": "vitest run"
  },
  "dependencies": {
    "@sattori/shared": "workspace:*",
    "aws-cdk-lib": "^2.261.0",
    "constructs": "^10.4.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "aws-cdk": "^2.1020.0",
    "esbuild": "^0.24.0",
    "tsx": "^4.19.0",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}
